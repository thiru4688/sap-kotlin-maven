spring:
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 2000ms
  cache:
    type: redis
#  datasource:
#    url: jdbc:mysql://localhost:3306/graphql_demo
#    username: admin
#    password: admin
#  jpa:
#    hibernate:
#      ddl-auto: update
#    show-sql: true

  graphql:
    path: /api/users
    graphiql:
      enabled: true
      endpoint: /api/users

server:
  port: 8080
