

type User {
    id: ID!
    name: String!
    email: String!
    createdAt: String!
}

type Query {
    user(id: ID!): User
}

type Mutation {
    createUser(
        id: ID!
        name: String!
        email: String!
    ): User
}

