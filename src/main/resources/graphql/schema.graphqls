type User {
  id: ID
  name: String
  email: String
}

type Query {
  users: [User]
  allUsers: [User]
  userById(id: Int): User
}

type Mutation {
  createUser(id: Int, name: String, email: String): User
   updateUser(id: Int!, name: String!, email: String!): User
  deleteUser(id: Int): Boolean
}